import { cva } from "class-variance-authority";

export const cvaTitle = cva (
    [
        "text-6xl text-white font-title"
    ]
);

export const cvaText = cva (
    [
        "text-3xl text-white font-text"
    ]
);